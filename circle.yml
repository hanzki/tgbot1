machine:
  python:
    version: 2.7.10

checkout:
  post:
    - pip install -U pip setuptools virtualenv

dependencies:
  pre:
    - pip install ansible

deployment:
  staging:
    branch: master
    commands:
      - ansible-playbook -i "localhost," -c local helloworld.yml
