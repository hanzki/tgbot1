machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - pip install --upgrade setuptools virtualenv
    - pip install ansible boto

deployment:
  staging:
    branch: master
    commands:
      - which python
      - echo $PYTHONPATH
      - echo $PATH
      - ansible-playbook helloworld.yml -i hosts -c local
