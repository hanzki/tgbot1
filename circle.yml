machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - pip install --upgrade setuptools virtualenv
    - pip install ansible boto

deployment:
  staging:
    branch: master
    commands:
      - zip -r output.zip .
      - ansible-playbook ansible.yml -i hosts -c local -e "ansible_python_interpreter=$(which python)"
