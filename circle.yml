machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - pip install --upgrade setuptools virtualenv
    - pip install ansible

deployment:
  staging:
    branch: master
    commands:
      - ansible-playbook -i "localhost," -c local helloworld.yml
